# ADR — Senna93 Fases 0-3: rede de teste, números, apresentação e a virada do cabo

**Data:** 27-28/08/2026 · **Merge:** master `e365426` (--no-ff, 66 arquivos, +6201/−988)
**Decisor:** Sergio (esqueleto, vetos, textos, teste ao vivo, merge) · **Esteira:** SDD
(29 tasks + fix waves, revisor adversarial por task, mutation-testing nos pontos críticos)
**Spec:** `docs/superpowers/specs/2026-08-27-senna93-unificacao.md` (anotada — V13/V4
supersedem as seções normativas antigas)

## O que está no ar (produção, flag DESLIGADA)

- **Placar:** unit **576** · api 212 · rules 142 · wader 104 · tsc/build limpos.
  Contrato da Ponte: **32 invariantes** (era 8 contratos/4 invariantes na S5).
- **F0** — 72 casos do Senna90 na esteira; baseline clínica pinada; 9º contrato
  (refluxoPulmonar); teste de pureza do motor.
- **F1** — frases curadas: aorta ACC/AHA (aneurisma ≥45 com índice cm²/m; arco ≤40/>40
  sem graus; WASE ♀≥66=38), GLS 3 faixas (contradição B1 morta), TAPSE >17, LAVI Lang,
  j22 sem buracos, estenoses área-primária/pior-grau, esclerose com frase, B4/B9/B21,
  Wilkins (não-avaliado + literal + descrições do artigo), massa +0,6g, HVE 115/95,
  bandas exatas da FE, adapter único (B30).
- **F2** — módulo de apresentação: isOOR/refVal por sexo+idade (19 linhas), valorTabela
  (vírgula, truncar, VIDE), rodapé por domínio, alerta SEXO_AUSENTE. cutoffs.ts morto.
- **F3** — A VIRADA: kill-switch `leo:params-engine`; com ON o Senna93 pinta
  `#params-tbody` (12 linhas), `calc-*`, `#out-*` e 5 alertas estruturados nos MESMOS
  nós DOM; ponte falha = toast + emissão bloqueada (frescor vigiado); proveniência
  `motorNumeros` gravada NA TRANSAÇÃO; refluxoPulmonar migrado (3 call-sites); banco de
  frases em React (chave `medcardio_banco` intacta). **Com OFF (produção): byte-idêntico
  ao comportamento anterior — provado hunk a hunk.**

## Teste ao vivo (27/08, Sergio + controller no browser, conta Gmail, flag ON por device)

8/8 itens aprovados: tabela 12 linhas com VR/realce por sexo+idade (números idênticos
aos pins), realce da coluna direita acendendo pela 1ª vez, alertas estruturados
aparecendo/sumindo, aorta nova no laudo, VIDE, emissão com travas + cancelamento
devolvendo crédito, kill-switch curando ao vivo. **Achado de ouro:** o realce do motor
LEGADO sempre foi deslocado 3 linhas (nunca visto porque nunca teve CSS) — o CSS novo
foi escopado a `[data-engine="senna93"]` e o bug do legado volta a ser invisível até
morrer na F5. PDF não gera em dev (bucket — config conhecida, produção ok).

## Decisões do Sergio no teste (V13/V4 FECHADAS)

1. **"O laudo é técnico e DESCREVE; sugestão de conduta é do médico assistente."**
   Morreram: a nota cirúrgica ("sugere-se avaliação cirúrgica…") e a frase de
   angio-TC/RM — em achados E conclusões. A flag `notaCirurgica` (≥50/≥55) fica
   estruturada, sem texto. REGRA PERMANENTE para qualquer frase futura do motor.
2. **Aneurisma com medida:** "Dilatação aneurismática da Raiz aórtica medindo X mm".
3. **Mitral pela diretriz** (pesquisa Perplexity 27/08): ASE/EACVI 2017 — área
   <1,0 importante · ≤1,5 moderada · ≤2,0 **leve direto** (gradiente só confirmatório;
   a exigência de grad≥5 na faixa caiu) · >2,0 silêncio. ACC/AHA 2020: >1,5 = Stage B.

## Gates antes de ligar a flag em produção (F5a)

Smoke offline (emissão bloqueia com aviso) · cartão do kill-switch no runbook
(`localStorage.setItem('leo:params-engine','off')` + F5) · e2e item 8 ramificado pela
flag · re-teste do modal do banco (no teste ao vivo não fechou ao inserir; código
está correto — hipótese: build antigo; se reaparecer, investigar fora do BancoFrases).

## Incidentes/lições da fase (para qualquer sessão futura)

- Caminho Windows cru (backslash+hex) em arquivo versionado que o Tailwind v4 varre
  (docs/, src/) MATA o build (escape CSS). Usar barras normais.
- `git add -A` PROIBIDO neste repo (backup de cache quase foi commitado; resets
  relativos são perigosos — usar SHA explícito).
- Commit inline do controller só com `fail 0` LITERAL conferido.

## Próximo

**F4 — sombra persistida**: retroativo grava em `workspaces/{ws}/privado/shadow-execucoes`
via Admin SDK (sem regra nova), allowlist = `docs/planos/2026-08-27-senna93-divergencias-esperadas.md`
(a F4 herda TODOS os flips declarados de F1-F3), meta 0 divergências inesperadas.
Depois: F5a virada (env `NEXT_PUBLIC_PARAMS_ENGINE=senna93`) → F5b aposentadoria do legado.
